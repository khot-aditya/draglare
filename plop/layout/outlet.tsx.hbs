import { FC } from 'react';
import PropTypes from 'prop-types';
import type { {{pascalCase name}}LayoutProps } from './types'
import { Outlet } from 'react-router-dom';

const {{pascalCase name}}Layout: FC<{{pascalCase name}}LayoutProps> = ({ children }) => {
    return (
        <div>
            {children || <Outlet />}
        </div>
    );
};

{{pascalCase name}}Layout.propTypes = {
    children: PropTypes.node
};

export default {{pascalCase name}}Layout;
